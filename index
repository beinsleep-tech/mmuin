<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Загрузка...</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <script>
        Telegram.WebApp.ready();

        // Твой секретный ключ (можно менять для разных реклам)
        const validKey = 'bull2'; // ← Замени на свой ключ

        // Username основного бота (без @)
        const mainBotUsername = 'твой_основной_бот'; // ← Замени на реальный

        const startParam = Telegram.WebApp.initDataUnsafe.start_param;

        if (startParam && startParam === validKey) {
            // Редирект в чат с основным ботом
            Telegram.WebApp.openTelegramLink(`https://t.me/${mainBotUsername}`);
            
            // Если хочешь сразу открыть Mini App основного бота:
            // Telegram.WebApp.openTelegramLink(`https://t.me/${mainBotUsername}?startapp`);

            // Можно закрыть текущую веб-страницу после редиректа
            // Telegram.WebApp.close();
        } else {
            // Если ключа нет — показываем нейтральное сообщение
            document.body.innerHTML = `
                <div style="text-align:center; margin-top:100px; font-family:sans-serif; color:#888;">
                    <h3>Ничего здесь нет</h3>
                    <p>😏</p>
                </div>
            `;
            Telegram.WebApp.expand();
        }
    </script>
</head>
<body style="background:#fff; margin:0;">
    <div style="text-align:center; margin-top:100px; font-family:sans-serif;">
        <p>Перенаправляем...</p>
    </div>
</body>
</html>
